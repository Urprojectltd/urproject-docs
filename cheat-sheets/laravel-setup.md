# Laravel Setup Cheat Sheet

## Basic Commands
- `composer install`
- `php artisan migrate`
- `php artisan serve`

## Docker Workflow
- `docker compose up -d` → start stack
- `docker compose down -v` → stop & reset stack

